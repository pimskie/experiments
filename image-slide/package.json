{
  "name": "image-slide",
  "version": "1.0.0",
  "description": "image slides",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "run-p watch serve",
    "build:styles": "sass --source-map src/css:dist/css",
    "serve": "browser-sync start --server --files 'index.html, dist'",
    "watch": "run-p watch:*",
    "watch:styles": "npm run build:styles -- --watch"
  },
  "author": "pimskie",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.26.7",
    "npm-run-all": "^4.1.5",
    "sass": "^1.26.5"
  }
}
