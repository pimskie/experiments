export const SCRATCH_CANVAS_SELECTOR = '.js-scratch-canvas';

// higher the number, the less frequent particles will spawn
export const PARTICLES_SPAWN_THRESHOLD = 0.7;

export const DEFAULTS = {
  eraserRadius: 30,
  circleCount: 10,
  coverColor: '#c4c4c4',
  particleColor: '#b0b0b0',
  revealThreshold: 0.7,
  sampleStep: 4,
};
